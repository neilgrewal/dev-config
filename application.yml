# ===================================================================
# NSG Spring Cloud Config.
# ===================================================================

# Property used on app startup to check the config server status
configserver:
    name: NSG Registry config server
    status: Connected to the NSG Registry config server!

# Default JWT secret token (to be changed in production!)
jhipster:
    security:
        authentication:
            jwt:
                base64-secret: ZhmQVlCJhdEpy5CgTL3XlwMXo4zr1MyLF9JjFxP1daeC71J16UA0eWn2MaNXy4p4o0CCM7uvUayrgXg2ZbGSnw==               

# Enable /management/logfile endpoint for all apps
logging:
    path: /tmp
    file: ${spring.application.name}.log
